[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback: allow direct navigation to nested routes (React Router)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
